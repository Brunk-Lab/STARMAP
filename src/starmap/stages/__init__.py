"""STARMAP pipeline stages.

Import stage functions from their modules, for example:

    from starmap.stages.nmf import run_nmf_for_gene
    from starmap.stages.logodds import run_logodds_for_uniprot

This package keeps optional heavy dependencies lazy so base installation stays usable.
"""
